# Artifacts

Generated outputs such as trained models, metrics, reports, cached intermediates, and temporary files belong here.

Only placeholder files are tracked in git. Actual runtime outputs stay local by design.

For a curated map of the important evidence bundles, see `docs/artifact-index.md`.
